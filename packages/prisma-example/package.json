{
  "private": true,
  "scripts": {
    "dev": "ts-node --transpile-only src",
    "migrate:save": "prisma2 migrate save --name 'init' --experimental",
    "migrate:up": "prisma2 migrate up --experimental",
    "generate": "prisma2 generate",
    "postinstall": "npm run generate && npm run migrate:up"
  },
  "dependencies": {
    "@prisma/client": "^2.0.0-preview021",
    "graphql-yoga": "^1.18.3",
    "nexus": "^0.12.0-rc.11",
    "nexus-prisma": "^0.9.1-next.1"
  },
  "devDependencies": {
    "prisma2": "^2.0.0-preview021",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  }
}
